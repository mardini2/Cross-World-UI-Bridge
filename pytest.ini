[pytest]
# Goal: make the project root importable so tests can do "from app...."
pythonpath = .
[pytest]
pythonpath = .
filterwarnings =
    ignore:invalid escape sequence.*pywinauto.keyboard:DeprecationWarning
